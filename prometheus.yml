
global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: "ml_service"
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets: ["localhost:8000"]
    scrape_timeout: 10s
